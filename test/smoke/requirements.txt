# ST 测试运行所需的第三方 Python 库
# 由 run_st.sh 统一安装；已满足的依赖会被 pip 自动跳过，仅安装缺失项。
# 注意：torch_npu / mindspore 与昇腾 CANN 存在版本匹配关系，
#       在已预装对应版本的环境中不要用 --upgrade 强制升级。

packaging
numpy
pandas
torch
torch_npu
mindspore
